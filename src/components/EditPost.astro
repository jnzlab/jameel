---
import type { CollectionEntry } from "astro:content";
import IconEdit from "@/assets/icons/IconEdit.svg";
import { SITE } from "@/config";

type Props = {
  hideEditPost?: CollectionEntry<"blog">["data"]["hideEditPost"];
  class?: string;
  post: CollectionEntry<"blog">;
};

const { hideEditPost, post, class: className = "" } = Astro.props;

const href = `${SITE.editPost.url}${post.filePath}`;
const showEditPost =
  SITE.editPost.enabled && !hideEditPost && href.trim() !== "";
---

{
  showEditPost && (
    <a
      href={href}
      target="_blank"
      rel="noopener noreferrer"
      class:list={[
        "type-meta inline-flex items-center gap-1.5 text-ink-soft transition-colors hover:text-cobalt",
        className,
      ]}
    >
      <IconEdit class="size-4" aria-hidden="true" />
      <span>{SITE.editPost.text}</span>
      <span class="sr-only">(opens GitHub in a new tab)</span>
    </a>
  )
}
